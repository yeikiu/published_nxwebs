User-agent: *
Allow: /

# No path-shaped rules for /data-center, /news or /calendar: the app is
# hash-routed, so those live behind '#' (/#/es/news) and never exist as paths —
# GitHub Pages answers them with the 404.html SPA fallback, HTTP 404 included.

# Disallow admin pages from indexing
Disallow: /admin

# Point to sitemap
Sitemap: https://fight-radar.com/sitemap.xml

# Crawl delay (be respectful)
Crawl-delay: 1